import type { Metadata } from 'next';
import './globals.css';
import Navbar from '@/components/Navbar';
import Footer from '@/components/Footer';

export const metadata: Metadata = {
  title: 'Lazy Susan — Discover Family Restaurants That Need You',
  description:
    'Lazy Susan shines a light on struggling family-owned restaurants in Brooklyn and Queens before they disappear. Browse, discover, and support the spots that make your neighborhood special.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased min-h-screen flex flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
